import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.norandiaconu.wherewatch',
  appName: 'where-watch',
  webDir: 'docs',
  server: {
    androidScheme: 'https'
  },
  backgroundColor: "#000000"
};

export default config;
